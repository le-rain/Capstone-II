% Call this function to find sections in audio with good consecutive peaks
function [tstart, tstop] = audioPulse(audio, bpm)

% Read audio from video and find peaks
[peaks, time, x, y, Fs, minDist] = findAudioPeaks(audio, bpm);

% Find start and stop of good peak sections
[tstart, tstop, sectionI] = findPeakSection(peaks, time);

plotAudioSection(tstart, tstop, sectionI,audio, x, y, Fs, minDist);

end
